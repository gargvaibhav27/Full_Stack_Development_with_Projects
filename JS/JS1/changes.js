let gameName = "spiderman";
gameName = "batman";

console.log(gameName);
// const is used to declare variables that cannot be reassigned. Once a variable is declared with const, its value cannot be changed. This is useful for values that should remain constant throughout the program, such as configuration settings or fixed values. However, it is important to note that while the variable itself cannot be reassigned, if the variable holds an object or an array, the properties of that object or the elements of that array can still be modified.
const username = "hani";// const is used to declare a variable that cannot be reassigned. Once a variable is declared with const, its value cannot be changed. This is useful for values that should remain constant throughout the program, such as configuration settings or fixed values. However, it is important to note that while the variable itself cannot be reassigned, if the variable holds an object or an array, the properties of that object or the elements of that array can still be modified.
// username = "chaicode"; // This will throw an error because you cannot reassign a value to a variable declared with const.
console.log(username);
// username = "chaicode"; // This will throw an error because you cannot reassign a value to a variable declared with const.
// console.log(username);